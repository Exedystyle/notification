import Toast from './toast';
import Modal from './modal';

const _container = document.getElementById('cont');

const toast = new Toast;
const modal = new Modal;

// toast.render(document.body);

// setTimeout(function() {
//     toast.close()}, 5000);

// modal.render(document.body);

// setTimeout(function() {
//     modal.close()}, 5000);
